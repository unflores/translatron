%h1 Translation Keys
#translations
  - @translations.each do |translation|
    .translation_pair
      .absolute_path
        != "<span class='scope'>#{translation.translation_key.scope}</span>.<span class='tkey'>#{translation.translation_key.tkey}</span>"
      .save
        Save     
      .translation
        .master_definition
          = translation.master_translation.value
        %textarea.definition= translation.value
